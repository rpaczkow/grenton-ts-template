{
  "scripts": {
    "clean": "rimraf ./dist",
    "compile": "npm run clean && tstl",
    "bundle:clu": "luabundler bundle ../dist/Clu_OnInit.lua -p ../dist/?.lua -o ../dist/Clu_OnInit_bundle.lua",
    "build": "npm run compile && npm run bundle:clu"
  },
  "devDependencies": {
    "grenton-ts": "^1.0.0",
    "lua-types": "^2.14.1",
    "luabundler": "^1.2.3",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2",
    "typescript-to-lua": "^1.36.0"
  }
}
